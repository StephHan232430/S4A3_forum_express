<div class="card">
  <div class="card-body">
    <div class="row">
      <div class="col-md-4">
        <img class="image-responsive rounded-circle" src="{{user.image}}" style="width: 300px;margin-bottom: 25px;">
      </div>
      <div class="col-md-8">
        <h6>{{user.name}}</h6>
        <p>{{user.email}}</p>
        <a class="btn btn-primary" href="/users/{{user.id}}/edit" role="button">Edit</a>
      </div>
    </div>
  </div>
</div>